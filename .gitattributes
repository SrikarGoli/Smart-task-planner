# Set default behavior to automatically normalize line endings
* text=auto

# Force batch scripts to always use CRLF line endings so they work on Windows
*.{cmd,[cC][mM][dD]} text eol=crlf

# Force bash scripts to always use LF line endings so they work on Linux/Mac
*.{sh,[sS][hH]} text eol=lf

# Force Java source files to always use LF line endings (keep visible as text)
*.java text eol=lf

# Force XML files to always use LF line endings
*.xml text eol=lf

# Force properties files to always use LF line endings
*.properties text eol=lf

*.js linguist-detectable=false
*.html linguist-detectable=true

# Treat all files in target/ as binary and not to be processed
target/** -text

# Treat JAR files as binary
*.jar binary

# Treat image files as binary
*.{png,jpg,jpeg,gif,ico,svg} binary
